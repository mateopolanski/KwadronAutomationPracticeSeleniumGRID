package pageObject.pages;

import org.openqa.selenium.*;
import utility.*;

public class SearchResult extends BasePage {
    public SearchResult(WebDriver driver, PropertyManager propertyManager) {
        super(driver, propertyManager);
    }
}
